@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="d-flex justify-content-center align-items-center min-vh-100">
    <div class="d-flex gap-3">
        <a asp-page="/Catalog/Index" class="btn btn-primary btn-lg">Vino</a>
        <a asp-page="/Destilados" class="btn btn-secondary btn-lg">Destilados</a>
        <a asp-page="/Cafe" class="btn btn-dark btn-lg">Café e Infusiones</a>
    </div>
</div>
